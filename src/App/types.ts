export type CoinInfo = {
    name: string;
    value: number;
};

export type typeUser = {
    id: number,
    name: string,
    pass: string,
    email: string,
    lang: string,
    currency: string
}